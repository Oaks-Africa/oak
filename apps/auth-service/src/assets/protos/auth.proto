syntax = "proto3";

package auth;

service AuthController {
  rpc ValidateJwt (AuthJwt) returns (AuthUser) {}
}

message AuthJwt {
  string jwt = 1;
}

message AuthUser {
  string id = 1;
  string name = 2;
}
